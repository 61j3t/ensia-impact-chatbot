# Copy to .env.local and fill in. Next.js loads .env.local automatically.
#
# Same connection string as the bot's top-level .env. The dashboard runs
# read-only queries against the bot's Postgres instance.
DATABASE_URL=postgresql://ensia:ensia@localhost:5433/ensia_bot
